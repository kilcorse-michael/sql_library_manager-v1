extends layout.pug
block content
    h1 Update Book
    include error
    include form
    form(method='post' action=`/book/${book.id}/delete` onsubmit="return confirm('Do you really want to delete this book?');")
      p
        a.button(href='/') Cancel
      p
        input(type='submit' value='Delete Book')
